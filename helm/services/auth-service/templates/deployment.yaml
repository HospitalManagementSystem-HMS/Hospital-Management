apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: {{ include "auth-service.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    app: {{ include "auth-service.fullname" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  strategy:
    blueGreen:
      activeService: {{ include "auth-service.fullname" . }}-active
      previewService: {{ include "auth-service.fullname" . }}-preview
  selector:
    matchLabels:
      app: {{ include "auth-service.fullname" . }}
  template:
    metadata:
      labels:
        app: {{ include "auth-service.fullname" . }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.service.port }}
              protocol: TCP
          envFrom:
            - configMapRef:
                name: {{ include "auth-service.fullname" . }}-config
            - secretRef:
                name: {{ include "auth-service.fullname" . }}-secret
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 15
            periodSeconds: 10
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
